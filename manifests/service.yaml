apiVersion: v1
kind: Service
metadata:
  name: autodeployXX
  namespace: namespace-workflow-1714462536151
spec:
  type: LoadBalancer
  selector:
    app: autodeployXX
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000